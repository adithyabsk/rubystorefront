<%= form_with(model: return_request, local: true) do |form| %>
  <% if return_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(return_request.errors.count, "error") %> prohibited this return_request from being saved:</h2>

      <ul>
        <% return_request.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date %>
    <%= form.text_field :date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
